name: Build and Publish Remote-Vector-Index-Builder Core Snapshot Image to Docker

on:
  push:
    branches:
      - main
    paths:
      # This workflow owns building both the core image and (via the cascade
      # below) the api image. app/** is included here so that app-only changes
      # rebuild core and then api in a single ordered pipeline, rather than via
      # a separate, unordered api workflow. 
      - 'remote_vector_index_builder/core/**'
      - 'remote_vector_index_builder/app/**'
      - '.github/workflows/publish_remote_core_image.yml'
      - '.github/workflows/publish_remote_api_image.yml'

  workflow_call: # enables workflow to be reused
    inputs:
      nested:
        description: 'Set to true when the caller already holds the publish-snapshot-images concurrency lock.'
        required: false
        type: boolean
        default: false
    secrets:
      REMOTE_VECTOR_DOCKER_USERNAME:
        required: true
      REMOTE_VECTOR_DOCKER_ROLE:
        required: true

permissions:
  id-token: write
  contents: read

# Serialize image-publishing pipelines so a base-image cascade and a core/app
# push can never publish out of order or race for the shared snapshot tags.
# When called from the base-image cascade the caller already holds that lock, so
# the nested run takes a per-run group instead -- requesting the same group as
# the caller is a deadlock and GitHub cancels the entire run.
concurrency:
  group: ${{ inputs.nested && format('publish-snapshot-images-nested-{0}', github.run_id) || 'publish-snapshot-images' }}
  cancel-in-progress: false

jobs:
  build-and-publish-core-image:
    name: Build and Publish Remote-Vector-Index-Builder Core Snapshot Image
    if: github.repository == 'opensearch-project/remote-vector-index-builder'
    runs-on:
      group: selfhosted-gpu-runners
      labels: g62xlarge

    steps:
      - name: Checkout code
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Build Docker Image
        # --pull forces Docker to fetch the latest faiss-base-snapshot base image
        # from the registry instead of reusing a stale, locally-cached layer on
        # the self-hosted runner. 
        run : |
          docker build --pull -f ./remote_vector_index_builder/core/Dockerfile . -t opensearchstaging/remote-vector-index-builder:core-snapshot

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
        with:
          role-to-assume: ${{ secrets.REMOTE_VECTOR_DOCKER_ROLE }}
          aws-region: us-east-1

      - name: Retrieve Values
        id: retrieve-values
        run: |
          DOCKERHUB_PASSWORD=`aws secretsmanager get-secret-value --secret-id jenkins-staging-dockerhub-credential --query SecretString --output text`
          echo "::add-mask::$DOCKERHUB_PASSWORD"
          echo "dockerhub-password=$DOCKERHUB_PASSWORD" >> $GITHUB_OUTPUT

      - name: Login to DockerHub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          username: ${{ secrets.REMOTE_VECTOR_DOCKER_USERNAME }}
          password: ${{ steps.retrieve-values.outputs.dockerhub-password }}

      - name: Push Docker Image
        run : |
          docker push opensearchstaging/remote-vector-index-builder:core-snapshot
      - name: Runner Cleanups
        if: always()
        uses: ./.github/actions/docker-cleanup
        with:
          workspace: ${{ github.workspace }}
  
  # Trigger the build of core image since api image is changed
  build-and-publish-api-image:
    needs: build-and-publish-core-image
    uses: ./.github/workflows/publish_remote_api_image.yml
    secrets:
      REMOTE_VECTOR_DOCKER_ROLE: ${{ secrets.REMOTE_VECTOR_DOCKER_ROLE }}
      REMOTE_VECTOR_DOCKER_USERNAME: ${{ secrets.REMOTE_VECTOR_DOCKER_USERNAME }}